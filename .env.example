DATABASE_URL="postgresql://user:password@localhost:5432/scout?schema=public"
ANTHROPIC_API_KEY=
SERPAPI_KEY=
BLOB_READ_WRITE_TOKEN=
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
RESEND_API_KEY=
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optionnel — protege /api/cron/price-check en production (verifie le header
# Authorization: Bearer <valeur>). Laisser vide en local pour tester librement.
CRON_SECRET=

# Paywall (etape 6) — cle secrete + prix recurrents crees dans le Dashboard
# Stripe (mode test), plus le secret de signature donne par `stripe listen`
# en local ou par le webhook configure en production.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_DECOUVERTE=
STRIPE_PRICE_ESSENTIEL=
STRIPE_PRICE_PRO=
